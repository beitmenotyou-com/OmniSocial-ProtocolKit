# Example .env file for Protocol Hub
PORT=4000

# Optionally define default relay or identity paths
DEFAULT_DOMAIN=yourdomain.com
NOSTR_RELAY=wss://relay.nostr.example
ATPROTO_PDS=https://pds.example
