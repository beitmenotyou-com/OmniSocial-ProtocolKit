name: Post Weekly Digest to Discussions

on:
  workflow_run:
    workflows: ["Weekly Digest"]
    types:
      - completed

jobs:
  post-to-discussions:
    runs-on: ubuntu-latest
    steps:
      - name: Post weekly-digest.md to GitHub Discussions
        run: |
          echo "🚧 This is a placeholder. Use a bot with GitHub API to post to Discussions."
