OmniSocial ProtocolKit — GitHub Pages Docs Site

This archive includes:
- All GitHub Pages content (`/docs`)
- Guides, protocol specs, onboarding, proposals
- Swagger UI viewer (`docs/openapi.html`)
- `_data/navigation.yml` for sidebar navigation (optional)

To use:
1. Place contents in `/docs` directory of your GitHub repo
2. Enable GitHub Pages from `docs/` as root
